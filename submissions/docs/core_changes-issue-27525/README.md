# ease-sidebar — Collapsible Sidebar Menu with Smooth Accordion

## What does this do?

A React component for a collapsible sidebar navigation that expands on hover with smooth width transition. Icons remain visible when collapsed.

## How is it used?

```jsx
import CollapsibleSidebar from './CollapsibleSidebar';

function App() { return <CollapsibleSidebar />; }
```
