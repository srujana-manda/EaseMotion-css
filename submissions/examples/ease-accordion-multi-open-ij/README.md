# Accordion Multi Open

Accordion where multiple sections can be open simultaneously. Smooth max-height transition per section.

## Features

- Multiple accordion sections
- Sections open independently
- Smooth max-height transition
- Chevron rotation indicator

## Usage

Toggle `.amo-open` class on `.amo-item`. JS handles click toggling. CSS handles transitions.
