# Stagger Grid Item

Grid items animate in with staggered delays using custom properties.

## Features
- Staggered translateY and opacity transition
- `--animate` custom property toggles state
- Delays controlled by `--i` index

## Control
Click "Toggle Stagger" to animate grid items in sequence.
