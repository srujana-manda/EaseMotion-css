# ease-neon-border-glow

An element with a neon border glow effect on hover.

## Features
- Pure CSS, no JavaScript required
- Multiple layered `box-shadow` values create the neon glow
- Smooth transition on hover

## CSS Custom Property Control
Natively uses CSS `:hover` pseudo-class. No custom properties needed.
