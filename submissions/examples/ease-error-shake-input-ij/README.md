# ease-error-shake-input

An input component that shakes horizontally and turns red on error.

## Features

- Horizontal shake animation on error
- Red border color change
- Error message appears on trigger
- Controlled via `--error` CSS custom property

## Usage

Set `--error: 1` on the `.esi-field` element to trigger the shake and error state. Set to `0` to clear.
