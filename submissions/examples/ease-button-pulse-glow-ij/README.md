# Button Pulse Glow

Button with pulsing glow ring animation. Subtle box-shadow pulse behind the button draws attention.

## Features

- Button with pulsing glow ring
- Box-shadow pulse via @keyframes
- Smooth keyframe animation
- Pure CSS, no JS required

## Usage

Add class `.bpg-btn` to a button. CSS animation runs automatically.
