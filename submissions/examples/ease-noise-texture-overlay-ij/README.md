# ease-noise-texture-overlay

A noise grain overlay applied via a `::before` pseudo-element.

## Features
- Pure CSS, no JavaScript required
- Inline SVG noise filter (no external assets)
- Subtle shift animation for the grain texture

## CSS Custom Property Control
Natively animated via CSS `@keyframes`. No custom properties needed.
