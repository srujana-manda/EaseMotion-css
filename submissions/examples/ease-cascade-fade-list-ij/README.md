# Cascade Fade List

List items fade and slide up sequentially with staggered delays.

## Features
- Cascade fade-in + slide-up for each list item
- `--cascade` custom property controls state
- Incremental `transition-delay` via `--i`

## Control
Click "Toggle Cascade" to animate list items in sequence.
