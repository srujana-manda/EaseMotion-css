# Avatar Pulse Ring

Avatar with pulsing status ring. Ring animates with scale and opacity pulse via `--apr-color`.

## Features

- Avatar with pulsing ring
- Ring pulse animation via @keyframes
- Color coded status via `--apr-color`
- Pure CSS, no JS required

## Usage

Set `--apr-color` on `.apr-avatar`. The `.apr-ring` child animates automatically.
