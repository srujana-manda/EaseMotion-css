# Tooltip Fade Arrow

Tooltip that fades in on hover with arrow pointer. Supports top and bottom positioning.

## Features

- Tooltip fades in on hover
- Arrow pointer via border trick
- TranslateY entrance animation
- Pure CSS, no JS required

## Usage

Structure `.tfa-target` containing `.tfa-tip`. Add `.tfa-bottom` class for below positioning. CSS handles hover reveal.
