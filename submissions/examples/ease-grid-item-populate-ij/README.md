# Grid Item Populate

A 3x3 grid where items populate one by one with a staggered scale+fade entrance.

## Features
- Staggered `transition-delay` via `--i` custom property
- Scale and opacity transition
- Toggle-driven via `--populate` custom property

## Control
Click "Toggle Populate" to animate items in/out with stagger delays.
