# Wave Text Animation

Text letters wave like ocean in sequence with staggered animation delays.

## Features

- Per-character wave animation
- Staggered delay for ripple effect
- Color shift through wave cycle
- Two size variants

## Usage

Wrap each character in a `<span class="wta-char">` inside `.wta-wave`. Set incremental `animation-delay` on each span to create the wave.
