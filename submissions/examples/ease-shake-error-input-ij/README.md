# Shake Error Input

Input shakes left-right like an error notification when validation fails.

## Features

- Horizontal shake animation on error
- Red border highlight on invalid input
- Error message toggle display
- One-click re-triggerable animation

## Usage

Add `.sei-shake` class to `.sei-input` to trigger shake. Toggle `.sei-error` visibility for the message.
