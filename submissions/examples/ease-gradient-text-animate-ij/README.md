# ease-gradient-text-animate

Text with an animated gradient fill that continuously shifts.

## Features
- Pure CSS, no JavaScript required
- Uses `background-clip: text` for gradient fill
- `background-position` animation creates the shifting effect

## CSS Custom Property Control
Natively animated via CSS `@keyframes`. Adjust animation duration to taste.
