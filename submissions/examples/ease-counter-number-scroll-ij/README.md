# ease-counter-number-scroll

A number counter component that animates value changes with smooth transitions.

## Features

- Large bold number display
- Smooth visual feedback on value change
- Random increment and reset controls
- Dark theme with elevated card design

## Usage

Update the `.cns-number` text content programmatically to trigger the transition effect. No CSS custom property needed — the transition is built into the element.
