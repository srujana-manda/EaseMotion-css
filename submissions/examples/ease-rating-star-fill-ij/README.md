# Rating Star Fill

Star rating with animated fill on hover and click. Stars fill sequentially with color transition.

## Features

- Star rating with hover fill
- Click sets rating value
- Sequential star fill via --rsf-value
- Scale animation on hover

## Usage

Set `--rsf-value` (1-5) on `.rsf-stars`. CSS colors stars up to that index.
