# Skeleton Text Lines

Skeleton placeholder for text content with pulsing animation. Lines of varying widths with shimmer.

## Features

- Skeleton lines of varying widths
- Pulsing shimmer animation
- Border-radius on each line
- Pure CSS, no JS required

## Usage

Use `.stl-line` with modifier classes for width. CSS handles shimmer via @keyframes.
