# Image Pan Zoom

An image container with cinematic zoom and pan on hover using CSS transform.

## Features
- `transform: scale()` for zoom effect
- `translateX` for cinematic pan
- `overflow: hidden` clips the zoomed area
- Smooth hover transition

## Control
Hover over the image wrapper to trigger the zoom-and-pan effect.
