# Hue Rotate Hover

An image that rotates through hues on hover using CSS `filter: hue-rotate()`.

## Features
- Smooth `hue-rotate` transition on hover
- 360-degree color rotation
- Gradient placeholder visual

## Control
Hover over the image placeholder to see the hue rotation effect.
