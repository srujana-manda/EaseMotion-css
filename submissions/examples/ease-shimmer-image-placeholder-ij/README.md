# ease-shimmer-image-placeholder

A gray image placeholder with a shimmer sweep overlay.

## Features
- Pure CSS, no JavaScript required
- `::before` pseudo-element creates the shimmer effect
- Gradient `background-position` animation sweeps across the placeholder

## CSS Custom Property Control
Natively animated via CSS `@keyframes`. No custom properties needed.
