# Saturate Hover Image

A grayscale image that transitions to full color on hover using CSS `filter: grayscale()`.

## Features
- `filter: grayscale(1)` default state
- Smooth `:hover` transition to full saturation
- Gradient placeholder simulating a colorful image

## Control
Hover over the image placeholder to trigger the saturation effect.
