# Filter Blur BG

Blurs the background behind a card using backdrop-filter. Toggle `--blur` between `0` and `1` to control blur intensity.

## Features
- `backdrop-filter` blur transition
- Glassmorphism card design
- Toggle-driven animation via `--blur` custom property

## Control
Click the "Toggle Blur" button to switch the blur effect on/off.
