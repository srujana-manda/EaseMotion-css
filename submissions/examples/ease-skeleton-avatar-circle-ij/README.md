# ease-skeleton-avatar-circle

A circular skeleton loading placeholder with a pulse and ring animation.

## Features

- Circular avatar placeholder with gradient background
- Pulse opacity animation on the skeleton
- Spinning ring border effect
- Controlled via `--loading` CSS custom property

## Usage

Set `--loading: 1` on the `.sac-container` element to start the skeleton animation. Set to `0` to stop.
